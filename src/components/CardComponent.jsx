import React from "react";
import { useDispatch } from "react-redux";
import { addItem } from "../features/cart/CartSlice";
import '../css/CardComponent.css'

const cardComponent = ({ brand, name, price, image}) => {
    return (
    )
}